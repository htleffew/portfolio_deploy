---
export interface Props {
  src: string;
  alt?: string;
  caption?: string;
}
const { src, alt = "", caption } = Astro.props;
---
<div class="figure">
  <div class="frame">
    <img src={src} alt={alt} />
  </div>
  {caption && <figcaption>{caption}</figcaption>}
</div>
