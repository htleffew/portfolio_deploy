# Internal / private documents — never publish to the public site
netflix_positioning_memo.md

# Build artifacts and caches
__pycache__/
*.pyc
site/dist/
site/public/projects_index.json

# Internal authoring tooling (canonical copy lives in the private writing-standards repo)
.claude/

studio/
# graphify derived graph artifacts (regenerable; daily EstateGraphRefresh)
graphify-out/
